// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct FireBowArgs {
    pub client_tick: u64,
    pub origin_x: f32,
    pub origin_y: f32,
    pub origin_z: f32,
    pub dir_x: f32,
    pub dir_y: f32,
    pub dir_z: f32,
}

impl From<FireBowArgs> for super::Reducer {
    fn from(args: FireBowArgs) -> Self {
        Self::FireBow {
            client_tick: args.client_tick,
            origin_x: args.origin_x,
            origin_y: args.origin_y,
            origin_z: args.origin_z,
            dir_x: args.dir_x,
            dir_y: args.dir_y,
            dir_z: args.dir_z,
        }
    }
}

impl __sdk::InModule for FireBowArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `fire_bow`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait fire_bow {
    /// Request that the remote module invoke the reducer `fire_bow` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`fire_bow:fire_bow_then`] to run a callback after the reducer completes.
    fn fire_bow(
        &self,
        client_tick: u64,
        origin_x: f32,
        origin_y: f32,
        origin_z: f32,
        dir_x: f32,
        dir_y: f32,
        dir_z: f32,
    ) -> __sdk::Result<()> {
        self.fire_bow_then(
            client_tick,
            origin_x,
            origin_y,
            origin_z,
            dir_x,
            dir_y,
            dir_z,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `fire_bow` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn fire_bow_then(
        &self,
        client_tick: u64,
        origin_x: f32,
        origin_y: f32,
        origin_z: f32,
        dir_x: f32,
        dir_y: f32,
        dir_z: f32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl fire_bow for super::RemoteReducers {
    fn fire_bow_then(
        &self,
        client_tick: u64,
        origin_x: f32,
        origin_y: f32,
        origin_z: f32,
        dir_x: f32,
        dir_y: f32,
        dir_z: f32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            FireBowArgs {
                client_tick,
                origin_x,
                origin_y,
                origin_z,
                dir_x,
                dir_y,
                dir_z,
            },
            callback,
        )
    }
}
